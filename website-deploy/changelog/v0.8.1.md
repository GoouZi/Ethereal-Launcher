# Ethereal Launcher 0.8.1

- 下拉选择改用系统 `NSPopover`，支持悬浮反馈、点击外部关闭、键盘与 VoiceOver，同时保留按下鼠标立即响应。
- 为下拉内容加入遵循“减少动态效果”的弹簧过渡，使打开与选项反馈更自然。
- 可滚动区域增加渐变遮罩和材质虚化，控件进入或离开可视范围时不再被生硬裁切。
- 默认窗口同比例放大到 1020 × 700，同时保留较小窗口和 macOS 12.4 的兼容能力。

